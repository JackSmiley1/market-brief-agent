name = "market-brief-invest"
main = "invest-worker.js"
compatibility_date = "2026-09-23"

# PIN and GITHUB_TOKEN are set as secrets (wrangler secret put), never in
# this file or anywhere in git — see README.md in this folder.
